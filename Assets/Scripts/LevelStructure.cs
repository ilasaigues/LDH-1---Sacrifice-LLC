using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelStructure : MonoBehaviour
{
    public GameObject wallContainer;
    [UnityEngine.Serialization.FormerlySerializedAs("spawneContainer")]
    public GameObject spawnerContainer;
    public GameObject processorContainer;
    public GameObject altarContainer;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
